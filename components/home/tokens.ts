// Tokens de la page d'accueil : alias de lib/theme.ts (source unique des couleurs et polices).
import { THEME_COLORS, THEME_FONTS } from '@/lib/theme';

export const C = THEME_COLORS;
export const F = THEME_FONTS;
